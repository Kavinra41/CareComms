package com.carecomms.android.di

import org.koin.dsl.module

val androidModule = module {
    // Mock implementations for Android-specific dependencies
    // These will be replaced with real implementations when Firebase is re-enabled
}